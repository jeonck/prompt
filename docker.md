[볼륨·포트만 잡으면, 도커 용어가 끝까지 연결됩니다]

볼륨
- 컨테이너 밖에 남겨두는 “데이터 금고”
- 컨테이너를 지워도 데이터는 유지되게 설계합니다.

포트 공개/매핑
- 컨테이너 안의 서비스 포트를
내 컴퓨터 바깥에서 접속 가능하게 연결하는 개념입니다.

AI에게 바로 써먹는 한 줄 프롬프트 3개
1) “Dockerfile과 compose.yaml 만들어줘. DB 데이터는 볼륨으로 영구 저장.”
2) “웹은 8080으로 공개하고, 컨테이너 내부 포트는 3000으로 설정.”
3) “이미지/컨테이너/볼륨/포트 설정을 한글 주석으로 설명해줘.”

